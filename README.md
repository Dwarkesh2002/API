# API
A simple REST API built using Node.js and Express for handling basic CRUD operations.
# Simple REST API

This is a simple RESTful API built using Node.js and Express.js.  
It performs basic CRUD (Create, Read, Update, Delete) operations.

## 🚀 Features

- Create data
- Read data
- Update data
- Delete data
- JSON-based responses
- RESTful routes

## 🛠️ Technologies Used

- Node.js
- Express.js
- PgSql
- Postman (for testing)

## 📂 Project Structure

